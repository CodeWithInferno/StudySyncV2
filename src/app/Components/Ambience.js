import React from 'react';

const Ambience = ({ isPopupVisible, togglePopup, onImageSelect }) => {
    return (
        <div>
            {isPopUpVisible && (
                <div>
                    
                </div>
            )}
        </div>
    );
};

export default Ambience;